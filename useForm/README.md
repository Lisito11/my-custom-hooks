# useForm Hook

Ejemplo de uso:

```
    const initialForm = {

        name: 'Lisanny',
        email: 'l.andrespg11@gmail.com'
        edad: '99',
    }
    
    const [values, handleInputChange, reset] = useForm( initialForm )

```